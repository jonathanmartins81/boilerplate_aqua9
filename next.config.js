/**
 * Configuração do Next.js
 *
 * Este arquivo define as configurações principais do framework Next.js
 * para o Boilerplate Aqua9.
 *
 * Funcionalidades:
 * - Configurações de build e otimização
 * - Configurações de desenvolvimento
 * - Configurações de produção
 * - Configurações de performance
 *
 * Nota: Atualmente mantido simples para facilitar customização
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configurações podem ser adicionadas aqui conforme necessário
  // Exemplos:
  // - experimental: { appDir: true }
  // - images: { domains: ['example.com'] }
  // - env: { CUSTOM_KEY: 'value' }
};

module.exports = nextConfig;
